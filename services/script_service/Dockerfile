FROM python:3.11-slim

WORKDIR /app

COPY services/script_service/requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY shared /app/shared
COPY services/script_service /app/services/script_service

ENV PYTHONPATH=/app

CMD ["uvicorn", "services.script_service.main:app", "--host", "0.0.0.0", "--port", "8000"]
